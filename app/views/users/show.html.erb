<%# <% provide(:title, "#{@user.full_name}(@#{@user.name})") %> %>
<%# <%= render partial: "show_format", locals: {user: @user, pictures: @user.pictures, posts: "list-active", favorites: ""} %> %>
<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>